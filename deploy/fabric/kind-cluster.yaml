# Single-node kind cluster for local dry-run.
# Forwards 80 → 30080 so we can curl http://localhost.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: cgc
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
